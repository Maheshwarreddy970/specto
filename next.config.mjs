/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['chatdocai.s3.ap-south-1.amazonaws.com','img.clerk.com','aceternity.com'],
      },
};

export default nextConfig;
