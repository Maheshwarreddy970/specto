import React from 'react';
import { Flag as Flagcomp } from 'lucide-react';

const Flag = ({ width, height }: { width?: string; height?: string }) => {
  return (
    <Flagcomp />
  );
};

export default Flag;
